import React from 'react';

//** styles */

export const Auth = () => {
    return (
        <form className={styles.auth}>
            <input />
            <button>Submit</button>
        </form>
    );
};

export default Auth;